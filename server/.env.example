# Backend Environment Variables

# Server Configuration
NODE_ENV=development
PORT=3001

# Database
MONGODB_URI=mongodb://localhost:27017/fitmate
# Production: mongodb+srv://username:password@cluster.mongodb.net/fitmate

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
SESSION_SECRET=your-super-secret-session-key-change-this-in-production

# Google OAuth (Get from Google Cloud Console)
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_CALLBACK_URL=http://localhost:3001/api/auth/google/callback
# Production: https://your-backend-url.onrender.com/api/auth/google/callback

# Frontend URL (for CORS)
CLIENT_URL=http://localhost:3000
# Production: https://your-frontend-url.vercel.app

# Instructions:
# 1. Copy this file to .env for local development
# 2. Replace all placeholder values with your actual credentials
# 3. For production, set these in your Render dashboard
# 4. Never commit .env files to version control
# 5. Generate secure random strings for JWT_SECRET and SESSION_SECRET
#    You can use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
